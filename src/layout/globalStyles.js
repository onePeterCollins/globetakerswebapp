//global styles include all custom css rules intended to be used throughout the UI for this project
//all global styles are imported into the 'globetakers' plugin

/**
 * @author -- Peter Collins cpnzereogu@gmail
 */

import Fonts from "./fonts.css"
import Styles from "./styles.css"
import Animations from "./animations.css"

export default {
    Fonts, Styles, Animations
}
